## Stripe Connect Account Prefill with Embedded Components

This demo shows some examples of prefilling account data using the `company: { ... }` and `individual:  { ... }` properties in `/v1/accounts` create and update calls.

